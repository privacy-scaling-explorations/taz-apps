{
    "name": "token-subgraph",
    "private": true,
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --product hosted-service dlrfc/taz-artwork-goerli",
        "create-local": "graph create --node http://localhost:8020/ dlrfc/taz-artwork-goerli",
        "remove-local": "graph remove --node http://localhost:8020/ dlrfc/taz-artwork-goerli",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 dlrfc/taz-artwork-goerli"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.30.4",
        "@graphprotocol/graph-ts": "0.27.0"
    }
}
